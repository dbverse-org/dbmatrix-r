# get_tblName

get_tblName

## Usage

``` r
get_tblName(dbMatrix)
```

## Arguments

- dbMatrix:

  A database-backed object inheriting from `dbData`.

## Value

A character scalar giving the DuckDB table name associated with the
object.
