# Matrix Transpose

Given a `dbMatrix` `x`, `t` returns the transpose of `x`.

## Usage

``` r
# S4 method for class 'dbMatrix'
t(x)
```

## Arguments

- x:

  `dbMatrix` object

## Value

`dbMatrix` object
